name: create-release


on:
  workflow_dispatch:
    inputs:
      RELEASE_TYPE:
        type: choice
        options:
        - patch
        - minor
        - major

permissions:
  contents: write
jobs:
  tagged-release:
    name: "Tagged Release"
    runs-on: "ubuntu-latest"

    steps:
      - uses: actions/checkout@v2.3.3
        with:
          fetch-depth: 0
      - name: Get previous tag
        id: previoustag
        uses: "WyriHaximus/github-action-get-previous-tag@v1"
        with:
          fallback: v1.0.0
      - name: 'Get next minor version'
        id: semvers
        uses: "WyriHaximus/github-action-next-semvers@v1"
        with:
          version: ${{ steps.previoustag.outputs.tag }}

      - name: Release patch
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.semvers.outputs.${{ inputs.RELEASE_TYPE }}}}
      
